# CattleSense Frontend Environment Configuration
# For local development with Vite dev server proxy, leave empty or set to '/api'
# For standalone production deployments (e.g. Vercel/Netlify pointing to remote backend):
# VITE_API_BASE_URL=https://your-backend-api.onrender.com/api
VITE_API_BASE_URL=/api
