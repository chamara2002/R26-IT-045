# Core deep learning framework (CPU build; ResNet50 classifier)
torch
torchvision

# Image processing
Pillow
opencv-python-headless
numpy

# Metrics / dataset splitting (used by the training script)
scikit-learn
pandas

# Flask backend
Flask==3.1.1
flask-cors==6.0.1
Werkzeug

# Utilities
requests
